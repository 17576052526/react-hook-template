import $ from 'jquery'
import './pager.css'  //如果有css，则把 css引用进来
/*
 jquery 分页插件，此处只是示例
 引用的时候 import '../plugins/pager'
 */
$.fn.pager = function (pageSize, dataCount, fun, pageNum, curIndex) {
    
}